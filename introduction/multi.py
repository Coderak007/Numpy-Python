# Multi dimensional array
# We can have more than one dimension in array. We can have 1D, 2D, 3D and so on.
# We can have as many dimensions as we want. But we will stick to 3D
# Matrix

import numpy as np

matrix = np.array([[1, 2, 3],
                   [4, 5, 6]])
                   
print(matrix)